# code-review 運用ルール・対象範囲

DiskLED は単独メンテナ・CI 無しのプロジェクトで、`/code-review` が唯一の品質ゲート（詳細は [docs/CONTRIBUTING.md](docs/CONTRIBUTING.md)）。無駄な調査に時間を使わせないため、目的と対象範囲をここに固定しておく。`/code-review` を実行する際は、このファイルの内容を前提とする。

## 目的

**最新のソースコードとスキン設定（layout.cfg 等）の正しさを見る。** 具体的には:

- Delphi ソース（`src/`）・asset-editor の JS（`asset-editor/`）・生成スクリプト（`tools/*.py`/`*.ps1`）のロジックの不具合
- `assets/*/layout.cfg` の設定内容の不整合・実装との齟齬（layout.cfg は `uSkinLoader.pas` が解釈する実動作データであり、通常のコードと同格に扱う）
- ドキュメント（`docs/PLANNED-*.md` 等）が実ソースの `file:line` と矛盾していないか

**生成物・参照専用データ・見た目だけのアセットの中身までは見ない。**

## 対象外（調査そのものをしない）

- **周辺アセットファイル**（PNG・ICO 等のバイナリ画像ファイル）。中身にレビュー可能なロジックが無いため。**ただし `layout.cfg` など、動作に関わる設定ファイルはここに含めない**（上記「目的」の通り対象）
- **自動生成コード・ビルド成果物**: `dist/`、`*.generated.*` に類するファイル（現状は `dist/` のみだが将来 asset-editor 側にビルド成果物が増えた場合も同様に除外）
- **旧版ソース**: `legacy/`（2.x の逆コンパイル・保存用ソース。参照専用で変更・出荷対象ではない）

これらのパスは `git diff` の対象に含めない・`grep`/`find`/`Glob` 等での探索対象からも外す。差分に含まれていても中身を開かない。

## 実行時の effort レベル運用

| タイミング | レベル |
|---|---|
| `work/...` ブランチ単位（1項目） | `low` |
| `feature/x.y.z` へのバッチレビュー（複数の `work/...` 分をまとめて） | `medium` |
| リリース直前の最終レビュー（`feature/x.y.z` → `master` 前） | `high` |
| `ultra` | 個別の項目が明確に高リスクと判断された場合のみ。既定では使わない |

`high`/`ultra` は調査の深さ・並列数が増える分、対象範囲が広いと時間がかかりやすい。上記の対象外ルールと組み合わせて、無駄な探索に時間を使わせないようにする。
